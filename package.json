{
  "scripts": {
    "build": "tailwindcss build -i styles/style.css -o public/styles.css --watch"
  },
  "dependencies": {
    "tailwind-config": "^0.1.2"
  },
  "devDependencies": {
    "@tailwindcss/forms": "^0.5.6",
    "tailwindcss": "^3.3.3"
  }
}
